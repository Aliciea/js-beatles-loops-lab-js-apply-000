// add solution here
function theBeatlesPlay(musicians, instruments) {
  
  var array = [];
  
  for(var counter = 0; counter < musicians.length; counter++){
    let string = musicians[i] 
    array.push(musicians[i]+ "plays" + instruments[i]);
  }
  return array;
}

