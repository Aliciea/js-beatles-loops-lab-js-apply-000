// add solution here
function theBeatlesPlay(musicians, instruments) {
  
  var array = [];
  
  for(var counter = 0; counter < musicians.length; counter++){
    const string = musicians[i] + "plays" + instruments[i]; array.push(string);
  }
  return array;
}

