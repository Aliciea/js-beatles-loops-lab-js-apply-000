// add solution here
function theBeatlesPlay(musicians, instruments) {
  
  var array = [];
  
  for(var counter = 0; counter < musicians.length; counter++){
    const string = musicians[i] + `"John Lennon plays guitar"` + instruments[i]; array.push();
  }
  return array;
}

